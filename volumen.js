const audio = document.getElementById("myAudio");
const adjustButton = document.getElementById("adjustVolume");

// Inicializar el volumen
audio.volume = 0;

// Mostrar el control de volumen al hacer clic en el botón
adjustButton.addEventListener('click', () => {
  // Aquí puedes mostrar un elemento con un control de rango para ajustar el volumen
  // Por ejemplo, utilizando una librería como jQuery o creando un elemento HTML dinámicamente
});